---
'@opencupid/frontend': minor
---

Load a runtime `/custom.css` stylesheet for local branding/theming, shipped as an empty placeholder so deployments can override it with a Docker bind mount without rebuilding the image.
